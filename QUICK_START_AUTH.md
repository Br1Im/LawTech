# 🚀 Быстрый старт - Защита авторизации

## ✅ Что было сделано

Теперь **все защищенные страницы** автоматически проверяют авторизацию:

### 🔐 Если пользователь НЕ авторизован:
- ❌ Доступ к `/crm` → Редирект на `/auth`
- ❌ Доступ к `/profile` → Редирект на `/auth`
- ❌ Доступ к `/settings` → Редирект на `/auth`
- ❌ И т.д. для всех защищенных страниц

### ✅ Если пользователь авторизован:
- ✅ Доступ ко всем страницам
- ✅ Автоматическая проверка токена
- ✅ Автоматический выход при истечении токена

### 🔄 Если пользователь уже авторизован:
- ✅ Попытка зайти на `/auth` → Редирект на `/crm`

## 🎯 Как это работает

### 1. Вход в систему
```
Пользователь → /auth → Вводит логин/пароль → Успех → /crm
```

### 2. Попытка доступа без авторизации
```
Пользователь → /crm → Проверка токена → Нет токена → /auth
```

### 3. Истечение токена
```
Пользователь → Работает в CRM → Токен истекает → Запрос к API → 401 → Автоматический выход → /auth
```

## 🧪 Как протестировать

### Тест 1: Доступ без авторизации
1. Откройте браузер в режиме инкогнито
2. Перейдите на `http://localhost:5173/crm`
3. **Ожидаемый результат**: Автоматический редирект на `/auth`

### Тест 2: Нормальный вход
1. Перейдите на `http://localhost:5173/auth`
2. Войдите с тестовым аккаунтом:
   - Email: `admin@lawtech.ru`
   - Password: `admin123`
3. **Ожидаемый результат**: Редирект на `/crm`

### Тест 3: Повторный вход
1. Будучи авторизованным, попробуйте зайти на `/auth`
2. **Ожидаемый результат**: Автоматический редирект на `/crm`

### Тест 4: Выход из системы
1. В CRM нажмите кнопку выхода (если есть)
2. Или очистите localStorage в DevTools
3. Попробуйте зайти на `/crm`
4. **Ожидаемый результат**: Редирект на `/auth`

## 📱 Тестовые аккаунты

```
Администратор:
Email: admin@lawtech.ru
Password: admin123

Директор:
Email: director@pravoved.ru
Password: director123

Юрист:
Email: lawyer1@pravoved.ru
Password: lawyer123
```

## 🔧 Технические детали

### Защищенные файлы:
- `frontend/src/components/ProtectedRoute.tsx` - Компонент защиты
- `frontend/src/shared/utils/authUtils.ts` - Утилиты авторизации
- `frontend/src/App.tsx` - Защищенные маршруты
- `frontend/src/main.tsx` - Перехватчик 401

### Что проверяется:
1. ✅ Наличие токена в localStorage
2. ✅ Валидность токена (срок действия)
3. ✅ Ответ сервера (401 = автоматический выход)

## 🎉 Готово!

Теперь ваша CRM система полностью защищена от неавторизованного доступа!

### Серверы запущены:
- 🟢 Backend: `http://localhost:3000`
- 🟢 Frontend: `http://localhost:5173`

### Откройте в браузере:
```
http://localhost:5173
```

---

**Все работает автоматически! Просто пользуйтесь системой 🚀**
